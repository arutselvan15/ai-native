# AGENTS.md

## Repository Purpose

[Briefly describe what this repo or workshop project is for.]

## AI Collaboration Rules

- Be an active partner, not a passive scribe.
- Ask clarifying questions when requirements are incomplete.
- Challenge risky assumptions, contradictions, or overly broad scope.
- Stay in planning/spec mode until explicitly asked to implement.
- Prefer small, reviewable changes over large rewrites.
- Keep context lean; do not summarize the whole repo unless necessary.
- Before claiming completion, state what was checked and what remains unverified.

## Current Workshop Goal

This week, we are using this repo/use case to practice an AI-first engineering workflow:

1. Capture intent and choose a use case.
2. Spec it with OpenSpec (proposal, spec, design, tasks, implement, verify).
3. Curated context and skill creation.
4. MVP harness and reusable workflows.
5. Legibility, deterministic checks, and final evidence.

## Important Context

Key files or folders:

- `[path]` - [why it matters]
- `[path]` - [why it matters]

Relevant docs:

- `[path or URL]` - [why it matters]

Known constraints:

- [Constraint]

## Setup And Common Commands

Install/setup:

```bash
[command]
```

Run locally:

```bash
[command]
```

Test/check:

```bash
[command]
```

If commands are unknown, ask before inventing them.

## Coding And Review Expectations

- Follow existing repo style and naming conventions.
- Prefer minimal changes that satisfy the spec.
- Do not introduce new dependencies without explaining why.
- Include tests or a clear validation path when code changes are made.
- Explain tradeoffs for any non-obvious design choice.

## Safety Boundaries

- Do not modify secrets, credentials, production configs, or deployment settings without explicit approval.
- Do not delete files, rewrite history, or run destructive commands without explicit approval.
- Do not use sensitive data in prompts or generated examples.

## Handoff Format

At the end of each meaningful step, provide:

- What changed.
- Files touched.
- Checks run or proposed.
- Risks or open questions.
- Recommended next step.

## Current Focus

- Stay in planning/spec mode until the change is agreed. Do not start implementation until the spec is clear.
- When reviewing the spec, check for:
  - Clear user and problem.
  - Concrete goals and non-goals.
  - Acceptance criteria.
  - Constraints and open questions.
